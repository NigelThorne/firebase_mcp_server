{
  "name": "firebase-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for Firebase Emulator - Firestore and Function logs",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "firebase-mcp-server": "dist/index.js",
    "firebase-emu-cli": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "keywords": [
    "mcp",
    "firebase",
    "firestore",
    "emulator"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "firebase-admin": "^12.0.0",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.0.0"
  }
}
